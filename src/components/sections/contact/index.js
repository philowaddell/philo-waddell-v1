import React from 'react';

import Section from  '@templates/section';

const Contact = (props) => {

  return (
    <Section {...props}>
      <h1>Contact</h1>
    </Section>
  )
}

export default Contact;
